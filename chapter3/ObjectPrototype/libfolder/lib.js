/**
 * Return passed value
 * @param  {number} num value to input
 * @param  {string} some string value that is not used
 * @return {number} returns same value
 */
function testfunction(num, str) {
  return num;
}

/**
 * Test descriptions
 * @param  {number} num returns number
 * @param  {number} number1 input boolean
 * @return {number} returns number
 */
function testfunction1111(num, number1) {
  return num;
}